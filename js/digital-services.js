const DIGITAL_DEMO=window.DIGITAL_DEMO||[];
const money=n=>n?new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(n):'Cek tagihan';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function loadDigital(){try{const r=await fetch('/api/digital-products');if(r.ok){const j=await r.json();if(Array.isArray(j.products)&&j.products.length)return j.products}}catch{}try{const m=await import('/catalog.js');return m.DIGITAL_PRODUCTS||[]}catch{return DIGITAL_DEMO}}
function initDigital(){const grid=document.querySelector('#digitalGrid');if(!grid)return;const tabs=document.querySelector('#digitalCategory');loadDigital().then(items=>{let cat='all';const paint=()=>{const list=items.filter(x=>cat==='all'||x.category===cat);grid.innerHTML=list.map(x=>{const unavailable=x.disabled;const href=unavailable?'#':`digital-checkout.html?service=${encodeURIComponent(x.slug)}`;return `<a class="game-card ${unavailable?'is-disabled':''}" href="${href}" ${unavailable?'aria-disabled="true"':''}><div class="game-cover"><div style="height:100%;display:grid;place-items:center;font:700 42px var(--font-display);color:var(--green)">${esc(x.icon||'◈')}</div></div><div class="game-body"><div class="game-meta"><span>${esc(x.category)}</span><span>${x.inquiryRequired?'Cek tagihan':'Digital'}</span></div><h3>${esc(x.name)}</h3><p style="color:var(--muted);margin:8px 0">${esc(x.description)}</p><div class="badges"><span class="badge accent">${unavailable?'Segera hadir':(x.inquiryRequired?'Inquiry dulu':`Mulai ${money(x.products?.find(p=>p.available!==false)?.faceValue||x.products?.find(p=>p.available!==false)?.price)}`)}</span></div></div></a>`}).join('')};paint();tabs?.addEventListener('click',e=>{const b=e.target.closest('button[data-category]');if(!b)return;tabs.querySelectorAll('button').forEach(x=>x.classList.remove('active'));b.classList.add('active');cat=b.dataset.category;paint()})})}
document.addEventListener('DOMContentLoaded',initDigital);
